import 'aos/dist/aos.css';
export const metadata = {
  title: "Elite Sugar - Edit Profile",
  description: "Edit Profile",
};

export default function RootLayout({ children }) {
  return <>
    {children}
  </>;
}