export const metadata = {
  title: "Sugar Elite - Register",
  description: "Create account for dating world!",
};

export default function RootLayout({ children }) {
  return <>
    {children}
  </>;
}