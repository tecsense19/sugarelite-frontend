export const metadata = {
  title: "SugarMake - Register",
  description: "Create account for dating world!",
};

export default function RootLayout({ children }) {
  return <>
    {children}
  </>;
}